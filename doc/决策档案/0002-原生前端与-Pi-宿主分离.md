# 原生前端与 Pi 宿主分离

状态：仍有效；扩展与 TUI 呈现边界由 [0005](0005-Pi-SDK-与原生呈现边界.md) 收窄。

`D Code` 使用 Swift 构建 macOS 原生界面，同时由独立 Pi Host 嵌入 Pi SDK，管理配置、会话、模型、工具和可兼容的结构化扩展交互。把完整 TUI 嵌入窗口无法提供 Plan、图表和产物的一等原生体验；因此两者通过受版本约束的本地协议交换结构化会话与呈现语义。代价是应用需要管理辅助运行组件、进程生命周期和版本兼容，但界面与运行边界保持清晰。D Code 不再承诺复用任意扩展的自定义界面；具体边界见 0005。
