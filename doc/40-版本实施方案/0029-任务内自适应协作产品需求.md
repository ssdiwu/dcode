# D Code 0.0.31 任务内自适应协作产品需求

状态：`0.0.31` 需求已确认；2026-09-07 起获 507 授权实施，当前按可独立验证能力在隔离工作树分批交付。由 [ADR 0045](../决策档案/0045-任务内自适应协调与智能体进程边界.md)固定进程边界。前一版 Web 客户端与灵感接入已形成可回退基线；本版实现完整任务内协作，并一并完成上一任务列明的迁移遗留。507 已授权每批验证和独立审查后创建本地 Git 提交；不推送、不合并主干、不发布。各批自动验证、实际窗口走查和人工验收分别记录，不能以部分提交宣称整个版本已完成。

## 版本定位与进入条件

`0.0.30` 完成当前客户端和灵感接入，`0.0.31` 完成本规格的任务内自适应协作；`0.0.32` 承接[工作台 UI/UX 升级](0030-工作台-UI-UX-并行候选.md)，多目录项目保留为后续待排期。本版内部按三个可验证阶段交付，不增加平行任务系统，不因拆成阶段而省略最终完整路径验收。

进入运行代码实施前须满足：

1. 前一版执行任务已提交明确的收口报告，相关自动检查、实际窗口路径和未覆盖边界记入 [PRD 0028](0028-0.0.30-Web客户端初版产品需求.md)；不能仅凭任务显示空闲或模型宣称完成判断。
2. 507 已确认前一版核心工作台和灵感流程，包括灵感保存、引用到任务与重启恢复。形成可追溯的本机候选、源码状态和可回退基线；提交、标签和发布仍按各自授权执行。
3. 本版从该基线在隔离工作树推进，避免与前一版同时改写运行核心和相同产品数据合同。先验证候选能力，再替换在役路径；历史任务、会话、附件和灵感引用需要继续可读可恢复。

版本冻结时不写死候选路径、进程空闲分钟数或交付日期。基础隔离、消息协作和验收闭环各自有完成信号，任何一项尚未通过时都不能宣布 `0.0.31` 已完成。

## 问题与目标

用户在一个任务的主对话中不断补充想法。若主智能体被具体执行占住，用户就无法持续讨论；若只得到一次子任务返回，又需要用户反复转述要求、协调多名成员和推动返工。

D Code 应让用户始终拥有当前任务的协调入口：简单工作直接完成，需要后台推进或分工时自主组织成员。用户可以与主智能体或任一子智能体对话，其他成员继续工作；成果经独立验收和协调者复核后交给用户。

## 本版范围与用户路径

| 用户要完成的事 | 本版行为 |
|---|---|
| 直接新建任务并持续讨论 | 协调者从首次对话承接，简单工作直办，后台工作主动派发 |
| 自动派发使用当前还能调用的模型 | 先确定该成员的模型回退链，再核验启用、访问、能力与配额；剩余不高于设置门槛或无法确认的候选不自动选中，沿链选择下一个合格模型 |
| 从灵感材料开始一个任务 | 复用前一版已经成立的灵感引用，协调者按被选中的内容版本工作，再向成员提供必要的有来源上下文 |
| 在成员工作期间补充要求 | 主对话继续处理输入；可以定向提及成员，也可以进入成员子对话，相关变化回流协调者 |
| 收到可信成果并继续修改 | 执行、独立验收、协调复核与局部返工串联，所有结果和证据归原任务 |
| 停止、回收后继续某个成员 | 单独控制其主执行进程，保留上下文、来源和结果，后续以新进程续接 |

灵感是输入来源之一，多智能体协作属于 Task，不能只在灵感页面可用。当前任务引用的灵感内容版本不随画布后续编辑静默变化；成员仅接收该任务选定的有界投影，创建团队不自动加载全部灵感或知识库。

界面沿用前一版已经验收的导航区、工作区和信息区布局。本版增加真实成员、定向消息、回复来源、状态及相关操作，不进行整套视觉换版，也不以未接通的按钮、模拟成员或工具名称完成验收。

## 与前一版迁移遗留的关系

507 在 2026-09-07 接受 0.0.30 基础功能后，要求把未迁移内容写清楚。[PRD 0028 的迁移遗留清单](0028-0.0.30-Web客户端初版产品需求.md) 是现状依据；旧 Swift 代码存在不等于原生任务主路径已经可用，本版不得仅通过搬运旧 Pi 客户端界面绕过 Product Store。

| 范围 | 0.0.31 的处理边界 |
|---|---|
| 运行中继续输入、成员请求与响应、执行来源及成果证据 | 属于本版既定协作目标。实现时明确消息何时接收、交给谁、何时处理，以及等待/失败反馈；补足完成协作验收所需的上下文、实际模型和证据可查性。旧队列或旧扩展弹窗只是可参考实现，不自动定义新合同。 |
| 项目文件树、多标签、Markdown/HTML 编辑、Git 状态与逐文件差异、会话路径重走、已有项目目录维护、通用技能命令选择 | 507 于 2026-09-07 明确要求把上一任务列出的六类未迁移能力一并完成，现纳入本次实施。全部通过 D Code 产品合同接入，不直接恢复旧 Pi 数据写入路径；Git 提交历史和终端仍属此前另列的规划能力。 |
| Artifact 文件正文、任务上下文选择和运行来源检查 | 纳入本次收尾：成员产物有可打开的正文或真实来源，任务可选择上下文并核对实际环境、工具与来源回执；报告正文和灵感版本引用继续保留。 |
| Swift 残留清理与旧打包退役 | 已迁移并通过验证的界面按依赖退役，保留品牌母版和必要共享合同。不得直接删除整个 app 目录；签名、公证、正式分发与自动更新仍按发布专项和独立授权处理。 |

迁移收尾与三阶段协作共同验收。实现时逐项记录源码、真实用户路径和剩余边界；任何尚未接通的旧功能不能因旧源码仍存在而勾选完成。

## 产品对象与职责

- Task 是用户可见的工作单位，可承载多段持续对话，主行进入该任务的 Coordination Session。单条用户消息或智能体回复是消息项，连续消息形成对话；Task 不等于某一条消息或其中任何一段对话。主会话和子会话使用不同稳定身份，不让每项子工作自动成为顶层 Task。
- 交互 Task 从首次提交起由该任务的 Coordinator Agent 承接；创建空草稿不要求启动模型或常驻进程。不同任务拥有各自的协调关系，不引入跨任务全局协调者。
- 一个 Task 可以包含多个 Work Item 和多个 Agent Run。执行、研究、验收等职责按工作需要配置；不固定一名执行者、一名验收者，也不为展示团队而启动无独立产出的成员。
- Coordinator 是角色，LLM 是其本次使用的模型。简单工作可以直接完成，转为专注协调时不更换任务或主对话；成员承担具体执行，协调者负责目标、派发、变更、冲突、复核与交付。
- 用户可直接进入成员的 Child Agent Session。选择成员、发送消息、返回主对话均不停止无关成员，不把正在观察的成员变成新的任务协调者。
- 运行成员只能由该任务协调者创建，用户可在主对话中要求它召集成员，不直接在界面新建运行成员；非协调成员也不能自行创建成员。用户编辑 Agent Profile 仍只是管理配置，不自动产生实例。主协调者由交互任务建立流程产生；已创建成员空闲回收后的进程重启不新建成员身份。

## 自适应派发规则

以下任一条件成立时，协调者应评估并在既有授权范围内主动派发，不等待用户反复要求组队：

1. 工作需要在后台持续推进，会妨碍主对话继续接收和处理用户表达；即使只有一项工作也适用。
2. 工作可以拆为具有独立完成信号的部分，分工能带来实际收益。
3. 工作需要不同专业判断或独立验收，不能只依赖执行者自述完成。

可在当前上下文中可靠完成、无需后台持续推进的小工作由协调者直接处理。不设“超过几步或几分钟就组队”的固定阈值，不把“存在多个 Agent 工具”作为组队理由。

派发前以简短可见说明交代分工与预期结果，随后实际启动成员；说明本身不是启动证据。任务范围内组队不增设每次用户确认步骤；改变目标、成本承诺、外部副作用授权或已有边界时，继续遵守相应用户决定与项目规则。

协调者需要实际具备本版允许的小工作执行、成员创建、派发、发送定向输入、停止受影响工作、安排验收与返工等能力；这些能力必须从真实注册工具和同一产品合同暴露。不能继续仅有只读协调角色，却用提示词宣称它可以直接完成简单写入工作或自主创建团队。成员创建边界由产品调用入口校验，不能只依赖模型遵守提示词。

每份 Agent Assignment 保留目标、范围、输入来源、完成标准、停止边界、回报对象和执行目录。提交原文与整理后的任务说明分开保存。多个成员可使用同一 Profile，但须分别建立执行身份、上下文、分工与进程绑定，不能共享一个在途执行实例。

并发数量受可用资源和既有设置约束。达到上限时工作明确等待，并保持协调对话可响应；不能为启动更多成员静默突破资源约束。干净且已有提交的 Git 项目中，多名写入成员继续使用独立且已验证的工作树。User Scope、普通目录、未提交初始化的仓库或含未提交内容的目录，保留原目录并按单写入者顺序执行，不为组队复制用户目录或替用户提交；协调者与只读成员仍可继续对话。目录占用同时检查父子重叠，用户文件保存与成员写入也遵守同一边界。

## 模型候选与配额感知派发

### 可用性筛选

自动创建成员、开始新的模型调用阶段或在安全边界续接执行前，按顺序核验：

1. 先确定该子智能体自己的有序模型回退链，包括首选与后续候选；不同成员可有不同链。链来自用户或 Profile 已允许的配置以及本次明确选择，候选引用 D Code Model Catalog；不强制所有角色共用一条链，不按模型名字猜测能力，不静默加入链外模型。
2. 先排除用户未启用的模型；对应 Provider 具有当前有效的访问条件，Runtime 能实际选择该模型，并满足本次任务需要的工具、输入类型和上下文要求。仅有目录条目或已配置凭据引用不等于此时可调用。
3. 从该连接实际使用的账户、套餐或额度池查询当前配额。只查询已经通过前两步的候选所涉及的来源，复用并合并相同额度池的请求，避免每个子进程分别查询同一份额度。
4. 沿链选择第一个已启用、访问与能力成立，且所有适用约束窗口的有效剩余额度均高于当前设置门槛的模型；不高于门槛时不再自动派发给它，直接检查下一个候选。保存链的生效版本、实际选择、跳过原因和配额快照来源，不按剩余百分比重新排序回退链。

507 于 2026-09-08 将固定下限改为设置项：在设置→模型选择整数 1–30%，默认 1%。设置由 Host Product Store 持久化，重启恢复，非法类型、越界值与小数拒绝保存。每次决策读取当时生效的门槛，同批成员预检共用同一值；后续实际投递、等待后重试、请求安全边界与相关配额评估再读取最新设置。修改不会重排回退链，不强制中断已经发出的请求、工具或工作；普通手动选择权限与未知额度处理沿用既有语义。比较使用有效原始百分比，不使用界面四舍五入后的展示值；例如门槛为 20% 时，20.01% 符合配额条件，20% 与 19.99% 均跳过。仅有绝对余额时，只有服务端明确给出同一适用额度池的总量才能计算比例，无法确定则为未知。高于下限仍不保证整项工作一定够用，启用、访问、任务能力、并发和其他已确认边界继续适用。

初版覆盖本地既有 `openai-codex`、`zai-coding-cn`、`minimax-cn` 查询路径所对应的账户连接。模型具体名称和版本从真实目录发现，不把用户举例的模型版本写成固定白名单；其他来源必须经过相同适配与验证才能声明支持。

### 配额状态与范围

| 状态 | 自动派发行为 |
|---|---|
| 有效快照确认所有适用额度窗口剩余均高于设置门槛 | 可以沿该成员既定回退链被选择；不保证额度足以完成整项工作 |
| 任一适用额度池或模型专属限额剩余不高于设置门槛 | 跳过该候选及受同一低额度池影响的其他候选，不在同池模型间无效轮换；低额度与实际耗尽分别显示 |
| 暂时限流或容量不足 | 与套餐耗尽分开记录，尊重可验证的恢复时间或等待策略，不能永久标为无额度 |
| 缺字段、过期、查询失败、未支持或适用额度池无法确定 | 明确标为未知，刷新或换用有有效状态的候选；默认不进入自动派发，不把未知显示为零余额或满额度 |

配额快照至少保留来源连接的安全身份、适用账户或额度池、适用模型/功能、时间窗口、服务端可提供的剩余量或剩余百分比、重置时间、抓取时间与错误状态。身份不含可反推出凭据的正文。Provider 只返回百分比时就显示百分比，不能推算并承诺“还能完成多少任务”。

不同窗口、不同功能额度分别建模。某项工作实际需要的任一约束窗口剩余不高于设置门槛就不能自动派发；无关功能低额度不应误伤普通文本调用。不能把所有 Provider 都强制转换成“5小时加每周”，也不能把同一订阅池误拆成每个模型各有一份额度。

快照过期或发生额度错误时先失效，再刷新；重置时间到了也需重新取得可用证据，不能仅凭本机时钟把额度改成满额。单个来源失败不阻断其他来源的有效结果。并发成员共享同一额度视图；额度请求合并、过期处理与运行反馈一起防止不断使用已耗尽的池。

### 回退与运行中的额度变化

配额在查询与调用之间仍可能变化。新快照到达设置门槛或以下，或收到可确认的额度耗尽错误后，更新相应模型或池的状态，停止向该池继续派发，并沿受影响成员各自的既定回退链选择其他合格来源。普通网络错误不能被当成耗尽，也不能触发无依据的永久禁用。

运行中的模型切换只发生在受控续接边界，保存真实模型与新调用事实，不热迁移在途生成、工具执行或阻塞交互。已经完成的动作不因回退自动重放；存在未知外部副作用时先核对并安全收口，再继续。

回退改变该成员后续使用的模型，不新建顶层 Task，不把它伪装成另一个用户可见成员，也不复制一个同时活动的主执行进程。任务职责、上下文与报告关系保持可追溯；如果 Runtime 需要重开，先安全结束旧进程，再建立新绑定并续接。

链内所有候选均不高于设置门槛、不可访问或无法确认时，工作进入明确等待/阻塞，显示原因及有依据的重置时间或恢复入口；不得声称成员已启动。该状态也可能影响协调者自身的模型响应，客户端需如实呈现，不能以“保持对话可用”伪造模型回答。不得静默升级套餐、购买额度、更换到未授权的按量计费账户，或为探测可用性发送未经授权的推理请求。

### 现有能力复用

优先使用 507 已有实现作为来源，不重新从零设计查询路径：

- [`pi-dusage`](https://github.com/ssdiwu/pi-dusage)：复用各 Provider 的只读查询、额度窗口归一化、版本化快照、抓取时间、安全错误、并发刷新合并和失败隔离逻辑。现有快照用于展示，接入自动路由前须补齐适用额度池、有效性及缺字段语义，不能把显示默认值当成可调用证据。
- [`pi-dteam`](https://github.com/ssdiwu/pi-dteam)：复用显式模型候选链、候选去重与回退次序的成立机制，接入前增加配额资格筛选；现有“能解析模型”不能等同“有额度”。不强制迁入其固定档位或旧团队所有权。
- [`pi-dstatus`](https://github.com/ssdiwu/pi-dstatus)：只作为结构化额度消费者的展示参考，不能从 Footer 文本反解析调度事实。
- [`pi-key-pool`](https://github.com/ssdiwu/pi-key-pool)：参考限流、容量、配额和网络错误的分类及停止重试机制，不在本版新增密钥池、改写 Pi 凭据或自动复制其固定冷却时间。

D Code 复用查询和归一化核心，凭据仍通过已有安全引用或 Runtime Auth Bridge 使用；不直接把依赖 `pi-tui` 的整个扩展入口装进产品，也不新增持续读取/同步 Pi 配置的产品权威。接口、字段与授权路径在实施时以当前来源和最小授权只读查询复验，不能仅凭旧 README 认定接口仍有效。

## 主对话、子对话与途中变更

主对话支持用户 `@` 同任务中已创建且可接收工作的成员。提及选择的是有稳定身份的现有成员，不按名字隐式创建；目标不存在、属于其他任务或已不可接续时给出明确反馈，用户可以改向协调者提出需求。此动作不提供绕过协调者的新建或克隆入口。

消息原文保存在发出它的对话中，单独记录实际发送者、目标成员和相关工作引用。目标成员收到定向输入，协调者同时收到有来源的指令与变更事件；无需用户再转述。成员对这条消息的回复回到原对话并显示真实成员来源，不伪装成协调者发言。消息出现在哪个对话不决定成员的进程身份，不因主对话和子对话分别收到消息就为同一成员同时启动两个主执行进程。

后台成员执行期间，协调者可以接收用户新消息、回答进度问题并调整任务，不等待整批成员结束才再次处理用户表达。客户端切换和模型运行是两件事；此承诺不包含 App 退出后继续运行。

主对话的补充材料或新要求应定向传给相关成员，在可接受输入的安全边界生效；不会原位覆盖已经保存的生效输入和执行证据。若旧方向不应继续，协调者停止或撤销受影响的后续工作，并明确哪些动作已经发生。

用户对子智能体直接补充或修改要求后，影响任务范围、进度、结论或其他成员依赖的变化应形成有来源的协作事件并通知协调者。保留原始消息，不伪造成主智能体或用户在其他会话重新发言。协调者不得因为同步较晚而用旧指令默默覆盖用户更新；涉及相互冲突或超出任务边界的意图时，只请求必要的澄清。

事件包含稳定身份、目标、相关工作与来源引用，避免重复送达造成重复执行。主对话无须复制成员完整聊天或私有过程；用户可以从状态、报告和来源回到对应子对话。主对话中的明显独立新目标应明确其归属，不静默扩展当前任务。

## 独立验收、复核与返工

- 需要独立验收的工作由不同于原执行者的 Agent Run 承担，可按成果类型组织多名验收成员。验收以任务标准、实际成果和可核验证据为依据，执行者自述不作为通过事实。
- 成员提交报告后，协调者复核任务覆盖、证据充分性、相互矛盾与用户最新要求，不能只读取“通过”标签即宣告完成。
- 成果有问题时回到相关执行成员返工，之后再次验收；验收遗漏或依据不足时，要求验收成员重新检查。不同工作项可独立循环，不无故重跑已经通过且未受影响的部分。
- 一般性返工在任务授权内继续推进，不要求用户反复回复“继续”。同类尝试连续两次没有新证据、也未缩小问题时，协调者必须调整方法或重新分工；确需改变目标、成本或风险时，再把具体决定交给用户。
- 验收通过、协调者复核、用户接受、测试通过、提交和发布分别成立。协调者向用户交付成果、必要证据与待决事项；正式 Task 接受沿用现有结构化验收请求，不能由成员或协调者文案代替。

## 智能体与进程约束

本规格使用根术语表中的 [Agent Execution Process（智能体执行进程）](../../GLOSSARY.md) 表达主执行锚点在运行期间的一对一关系，覆盖主智能体与所有成员：

- 一个正在运行的 Agent Run 恰有一个独立的操作系统主执行进程；该进程只承载这一个 Agent Run，同一智能体可以有多个可归属的工具辅助进程。
- 不允许多个智能体只以同一进程里的 Runtime 实例实现，也不允许同一 Agent Run 同时拥有多个智能体执行进程。
- 并行调度必须为主对话保留继续处理输入的能力，不能让所有可用运行名额都被成员占满后使协调者只能等待。具体资源管理方式由实施确定，并以真实并发输入路径证明。
- Profile、会话和历史 Run 可以没有活动进程；重启是旧进程退出后的新尝试与新绑定，不是创建第二个同时活动的执行者。
- Host、窗口、浏览器或命令工具的辅助进程不等于智能体实例，必须保持来源归属和生命周期可解释。
- 单独停止或异常终止成员不应中断协调者与无关成员。Host 仍拥有产品事实写入与外部副作用边界，进程隔离不产生新的 Product Store 所有者或权限模式。

## 空闲驻留、回收与上下文续接

主执行进程在本轮工作收口后可以短暂空闲驻留，以便继续交流；系统随后可以安全回收，无须让所有已完成成员长期占用进程。驻留时长不在本规格固定，不能因超时机械丢弃尚未保存的上下文或在途工作。

回收前必须确认上下文、报告和成果引用已经持久化，在途工具、阻塞交互和外部副作用已经收口或进入可解释的安全状态，并处理辅助进程。再次收到该智能体需要处理的工作时，使用持久化上下文及有来源的相关记忆启动新主执行进程，保存新进程绑定；旧会话和智能体身份保持可追溯，不自动重放完成操作。上下文丢失或无法恢复时明确报错，不只靠摘要伪装完整续接。

## 验收标准

- [ ] 创建交互任务后，主对话由该任务的协调者承接；简单工作无需组队即可完成，同一任务中的后续复杂工作不要求更换主对话。
- [ ] 不同成员可持有不同有序模型回退链；自动选择严格沿各自链的顺序，不按全局单一链或剩余百分比重排。
- [ ] 已启用且有访问条件、但当前适用剩余额度不高于设置门槛的模型被自动跳过；后续首个合格候选被实际使用。覆盖门槛 1% / 30% 上下界、等于门槛、略高/略低于门槛及界面取整差异。设置重启恢复，非法输入拒绝，原始配额缓存不因改门槛而伪造或失效。
- [ ] 共享额度池降至设置门槛或以下时，同池的不同模型全部正确退出相关自动候选；无关功能限额不误伤不使用该功能的任务。
- [ ] 缺字段、查询失败、过期和未支持来源显示为未知且不冒充可用；一个来源失败不影响其他来源。不存在有效候选时真实等待，不伪启动成员。
- [ ] 同一批多成员派发复用同池查询；额度在运行中耗尽时，错误回流并阻止后续派发，安全回退不重放已完成副作用、不热换在途运行，也不进入未授权付费来源。
- [ ] 重置或补充额度后经重新查询确认高于设置门槛 才恢复候选资格；账户连接、模型专属额度与各时间窗口映射经脱敏响应和最小授权只读查询验证，凭据不进入界面、普通日志或模型上下文。
- [ ] 交付一项需要后台持续推进的工作后，成员仍在执行时，用户能在主对话完成一次新消息交互；不能仅显示“运行中”就视为对话仍可用。
- [ ] 同一任务可按需运行至少两名执行成员和两名独立验收成员，职责与工作项可追踪；这个验收样例不成为固定人数配置。
- [ ] 用户能在任一成员子对话补充材料或要求，再返回主对话；原文、目标成员和任务变化可回查，无关成员继续运行，协调者能获知相关变更。
- [ ] 主对话中提及一个已创建成员并交办新要求，目标成员收到、协调者获知，成员回复回到主对话并标明来源；用户无需切页或重复转述，同一成员不会因来自不同对话的消息产生两个活动主执行进程。
- [ ] 没有成员时用户只能要求协调者创建，不能从新建成员按钮、提及未知名字、复制入口或非协调成员的调用绕过创建边界；用户编辑档案不产生运行成员。已有成员进程回收后的上下文续接不误建第二个成员身份。
- [ ] 延迟或重复送达的变更不会重复启动工作，也不会用旧要求覆盖已采纳的新要求；范围冲突明确暴露。
- [ ] 成果不合格触发执行返工，验收不充分触发重查；问题消除后重新验收并由协调者复核，不需要用户逐轮催促。
- [ ] 连续无进展的返工不会无限重放同一操作；方法调整、重新分工或需用户决定的边界有真实记录。
- [ ] 运行证据能把每个活动 Agent Run 对应到唯一、不同的操作系统执行进程，并证明进程只服务该实例；逻辑 Runtime 标识不能代替此证据。
- [ ] 单独结束一名成员执行进程后，其状态明确失败或停止，协调者和其他成员仍可工作；恢复前确认旧进程已终止，不制造同时活动的重复实例。
- [ ] 成员报告、验收报告、协调复核、用户接受与发布分别可辨认；产物及证据仍归原任务。
- [ ] 成员完成后经历空闲回收，再次进入该成员子对话发送补充要求，系统使用新进程正确续接已持久化上下文；原报告与消息保留，已完成动作不重复执行，辅助进程去留可解释。
- [ ] 资源不足时显示真实等待而非伪启动；停止与恢复不遗留无法归属的工具辅助进程或未知外部副作用。

## 实施依赖与切片规划

三个阶段都属于 `0.0.31`；本节定义逐步证明的能力与进入下一阶段的条件，不指定易变的内部文件或 API 名称，不以文档落盘自动授权实现。

1. **独立执行与模型供给基础**：先证明主智能体和成员的独立主执行进程锚点、辅助进程归属、事件归属、单独停止、空闲回收、上下文重新启动与故障隔离；同时复用已有模型与额度能力，证明候选资格、额度池映射和失效回退。Product Store 单写入不变，未通过前不替换在役 Runtime 路径。
2. **持续沟通与自主派发**：接入简单工作直办、协调者独占的成员创建、后台派发、主对话提及已有成员、直接子对话和变更回流；用成员执行期间真实完成新一轮主对话的动作验收。
3. **独立验收与返工闭环**：按工作项连接执行、验收、协调复核、局部返工与用户接受，验证多成员并行及停止条件，不以一次团队成功替代完整闭环。

| 阶段 | 必须证明的完整动作 | 不得作为替代证据 |
|---|---|---|
| 独立执行与模型供给基础 | 额度耗尽候选被跳过，有效候选实际启动多个独立主执行进程；单独停止、空闲回收、基于原上下文重开 | 仅目录中有模型、额度字段缺失按满额处理，或不同进程数量却没有稳定成员对应关系 |
| 持续沟通与自主派发 | 后台成员仍在执行时，主对话真实完成一轮新交互；提及成员并收到有来源回复；进入子对话改要求后协调者获知 | 输入框看起来可用，但消息只能等团队全部结束才处理 |
| 独立验收与返工闭环 | 多项工作分别执行与验收，一项故意不合格后局部返工、复验并回主对话；无关成果不重复重做 | 并行启动一次成功、执行者自称检查通过或汇总一段“全部完成”文字 |

每个切片单独记录实现和验收，通过后再推进下一切片；若新发现需要改变本版目标或已确认边界，回到该项决定，不把失败能力暗中移出验收清单。版本索引记录实施与发布状态。

## 完整版本验收与回归边界

完成本版前，至少在真实客户端中跑通一条组合路径：从一个已选定版本的灵感创建任务，协调者按需组织多名执行与验收成员；用户在执行中继续主对话、提及成员并进入一次子对话修改要求；一项成果验收失败后局部返工，协调者综合结果并发起用户验收；成员空闲回收后再续接。另以直接新建任务验证同一协作能力，不要求经过灵感入口。

组合路径应包含一项剩余额度不高于当前门槛的候选被跳过、沿同一成员回退链选择另一有效候选并实际使用的证据。耗尽、未知、共享池和途中限流可使用受控脱敏响应制造边界，实际有效候选的模型调用与任务结果仍须单独核验，不消耗真实账户额度来刻意制造耗尽。

自动回归使用隔离数据与可控运行反馈，覆盖进程身份、事件归属、原文与生效输入、定向消息去重、过期输入、失败传播和恢复。组合路径仍需有真实模型与工具活动的候选走查，不能只替换模型返回后宣布自主协作已经成立。真实账户和外部副作用使用明确授权的验证范围。

前一版的任务创建、草稿、切换、附件、灵感内容版本及引用、重启恢复和核心输入交互均须回归。任何数据升级先建立隔离恢复与回退方案，不假定旧客户端能够读取升级后的数据，也不以回滚 App 掩盖数据不兼容。

本版完成信号是：本规格所有验收项和上述组合路径成立，当前必要检查通过，507 完成人工验收，并留有候选及恢复证据。提交、推送、签名、发布分别授权和核验，不由 PRD 完成或自动检查推导。

## 非目标

跨任务全局协调者、固定三人团队、全量共享聊天上下文、自由无限互聊、云电脑、移动接入、App 退出后常驻服务，以及新增权限模式均不在本规格内。复制某名成员的在途上下文、分叉或合并执行血缘尚未形成独立功能合同；本规格中的多成员是分别创建实例，不把用户提到“复制”推导为完整克隆能力。

## 来源与当前证据边界

- 用户决定：2026-09-06 在“调研 Grok Bot 形态”中明确任务内协调者、简单工作直办、后台工作自主派发、多名执行与验收成员、用户直接子对话以及一个智能体对应一个独立执行进程；并明确授权写入项目规则和文档。
- 历史讨论：“D Code 任务、会话与多智能体协作模型”已经讨论塔台回收成员结果、任务拥有多会话；本规格不恢复其中已被当前权威取代的数据存储或版本建议。
- [MiniMax Agent Team 官方机制](https://minimaxi.com/blog/minimax-agent-team-long-running-1779893521)：主智能体组织工作、独立执行与验收循环、后台执行期间继续沟通。官方自述不等于本机桌面动态验证，也不证明其采用本规格的一对一进程关系。
- [Grok Bot 协作说明](https://docs.x.ai/grok-bot/chat-and-collaboration)：可见交接与多轮沟通参考，不引入 Bot 优先的顶层导航或云端权威。
- 本次源码调查：既有 Host 有多个 Runtime 的身份与团队规划、成员执行、综合报告路径；Web 客户端已消费成员、请求与报告。它们不能证明独立操作系统进程、自主动态派发、持续主子沟通和验收返工全链路已成立。
- 本地复用调查：已读取 `Githubs/pi-dusage` 查询与快照、`pi-dteam` 模型路由、`pi-dstatus` 快照消费和 `pi-quota-probes` 来源说明。现有 `pi-dusage` 两份测试中的 8 项在本轮通过，覆盖 Fast 刷新适配与 xAI 解析；不是 OpenAI、智谱、MiniMax 三条当前真实额度接口的验证，也不是 D Code 配额感知派发已实现的证据。本轮未读取用户凭据或查询其实际余额。

2026-09-06 的交付仅为规则和规格文档。2026-09-07 已开始下述隔离实现；App 验收、提交、推送和发布仍分别成立。

<details>
<summary>2026-09-07 早期候选记录（历史证据）</summary>

以下保留分批提交前的调查和失败记录；其中“未提交”“待完成”和当时测试数字只描述记录产生时的候选，不代表当前交付状态。最新事实以本页下方分批记录为准。

## 早期实施记录

前置核对：前一任务已由 507 接受基础功能并完成源码提交，起点为 `7956d70`，工作区干净。当前在隔离工作树实施，未提交。

- [ ] 独立执行与模型供给基础：真实进程、生命周期、配额查询、有序回退。
- [ ] 持续沟通与自主派发：主子消息、定向提及、成员创建、原生输入请求。
- [ ] 独立验收与返工闭环。
- [ ] 文件树、文件标签、行定位、Markdown/HTML 编辑、保存冲突和即时预览。
- [ ] Git 状态、逐文件差异、变更片段与引用。
- [ ] 会话路径、编辑旧消息重走和历史节点续写。
- [ ] 运行中输入、后续消息的编辑/排序/暂停与恢复，复用协作输入事实。
- [ ] 技能/命令选择及标准扩展选择、确认、输入、编辑交互。
- [ ] 已有项目改名、目录更换与有界迁移；不混入多目录项目。
- [ ] 任务上下文、运行环境/工具/来源回执检查和产物正文。
- [ ] 已替换 Swift 界面及旧桥/打包路径退役；品牌与共享合同保留。
- [ ] 全量回归、实际窗口与独立交付审查；包括之前未通过的物理拖放走查。

### 已取得的候选证据

以下均来自 `codex/0.0.31-adaptive-collaboration` 隔离候选，以 `7956d70` 为起点，未替换已验收基线。自动检查使用临时 Product Store 与受控供应商响应，不代表真实账户额度或真实模型自主决策已验收。

- `host/test/process-agent.test.ts`：两个 Pi 循环使用不同操作系统 PID，Host 工具回调保留运行身份；空闲回收后新 PID 续接原上下文；结束一名成员不影响另一名；清空队列及回收边界续接。
- `host/test/model-quota.test.ts`：已通过 11 项，覆盖原始 1% 阈值、全部适用窗口、未知字段、模型专属池、功能限额、查询合并、错误隔离、来源域名与供应商限流后的冷却。查询逻辑从本地 `pi-dusage` 参考迁入，自动候选沿成员档案顺序选择。
- `host/test/adaptive-collaboration.test.ts`：实际 Pi 工具循环先完成一项文件写入，再创建两个不同 PID 的后台成员；主对话继续回应，并在原团队工作中增派第三名成员；重复派发复用成员，低额度候选跳过；定向补充得到带成员身份的回复，生成消息不会被写成用户原话，因果原文引用固定保留。测试返回由受控供应商生成，未宣称真实模型的自发组织能力已验收。
- `host/test/collaboration-store.test.ts`：重启后未发送消息暂停、已开始消息标为中断，旧进程记录终止；定向草稿的文字和目标成员一起恢复。
- `host/test/collaboration-verification.test.ts`：验证报告必须使用本人检查产生的证据，拒绝自验、其他成员证据、旧版报告和旧证据；无进展返工要求记录方法调整；协调复核仅更新对应工作项，不替用户接受任务。尚需实际工具循环与真实客户端的组合验证。
- 原客户端 `ui-flow.test.mjs` 的创建、流式、草稿切换、停止、恢复与模型切换重新通过；旧用“发送按钮出现”表示运行结束的断言，改为读取真实运行完成状态，发送按钮现在在运行期间保留。
- Chromium 共享输入检查通过 38 个输入定义、196 个状态；既有 40 种窗口布局检查通过。首次候选界面走查验证了成员菜单、名称筛选与定向对象，发现并修复了定向草稿目标丢失、缺少键盘选择和内部通知出现在普通对话区的问题；修复后的完整视觉复验仍待完成。

阶段完成项暂不勾选。待收口包括：写入成员及辅助进程的完整生命周期、User Scope 的后台写入边界、运行中安全回退、输入队列编辑/排序/即时介入与延迟变更冲突、派发部分失败的恢复、完整独立验收返工组合路径，以及本版已纳入的六类迁移遗留、上下文/产物检查和旧 Swift 客户端退役。最终必要检查与人工验收仍须绑定最终候选。

### 文件、普通目录协作与消息路径增量

- 文件工作台已接入原生安全文件辅助程序，支持文件树、多标签、行定位、Markdown 编辑与冲突处理；实际候选窗口已验证冲突拒绝、明确覆盖和未保存修改提示。大小写路径别名与符号链接边界有独立回归。
- HTML 采用独立无持久存储的浏览器会话，消费编辑缓冲；实际窗口已验证内联脚本、本机样式、编辑后更新及刷新不抢输入焦点。`client/test/main/html-preview.mjs` 进一步通过默认断网、本次放行、切文件恢复阻止、App API 隔离、弹窗阻止及旧编辑器清理不能关闭新预览。
- `adaptive-collaboration.test.ts` 已增加 User Scope 与其下嵌套 Project 的实际 Pi 写入循环：三个成员拥有不同 PID，重叠目录的写入依次进行，主对话继续回复；父目录成员运行时子目录文件保存被拒绝，工作结束后自动接续并释放目录。等待后重新检查额度会记录新的选择事件，不复用旧决策的幂等键。
- `collaboration-store.test.ts` 已增加暂停后编辑、队列重排和重启恢复；修改前后提交原文分别保留，重复 ID、过期版本、正在处理的消息不能被重排或改写。界面已接入相应操作，仍需最终窗口组合走查。
- `native-session-path.test.ts` 已通过编辑旧问题、从旧回答继续、原路径回看、重启保留路径和过期路径操作拒绝；原始输入与修订输入分开，已完成的写入不会因路径变动再次执行或被撤销。新路径界面、草稿备份及导入历史的特殊路径仍需进一步验证。

以上仍为未提交候选。待完成项继续包括运行中即时补充、安全模型回退、派发部分失败恢复、完整多成员独立验收循环，以及技能/标准扩展交互、项目目录维护、上下文/产物正文和 Swift 退役。旧自动检查数字只对应其当时范围，最终交付以全部增量之后的复验为准。

### 持续输入、回退与辅助进程复验增量

- `collaboration-steering.test.ts` 三项通过：主对话即时补充在原 Session Run 和 PID 内消费，原文/生效输入分别保留；成员已收到用户新要求后，延迟的旧交办被拒绝，协调者读取新要求再发送修订安排；停止前未消费的补充保留为中断，不被 Pi 自动续跑。实际 Provider Call 的输入关系在调用开始时固定，不用结束时的新输入冒充此前已见内容。
- `quota-fallback-runtime.test.ts` 两项通过：一个写入成员在已完成写入之后收到 HTTP 429，沿原回退链切换模型，在同一成员/进程中继续且不重复写入；手动上下文压缩也按配额筛选并保留独立调用记录，不伪造新的用户运行，不将摘要提示词写成主智能体身份。拆分摘要的后续调用继续跳过已限流来源。
- `auxiliary-process.test.ts` 与 `auxiliary-runtime.test.ts` 六项通过：命令由带独立身份的进程组管理，普通命令结束后回收；后台子进程继续归属原成员，主循环空闲回收和新 PID 续接不丢失后台归属；单独停止不会影响另一成员的组。控制进程异常消失而子进程仍存活时保留未知状态和写入阻挡，不以控制进程退出冒充整组结束。客户端已添加后台活动及停止入口，最终窗口走查仍待完成。
- 本轮独立审查指出并已修复：中止后临时队列导致 Pi 自动续跑；摘要回退覆盖主身份；辅助进程扫描误计自身；控制进程异常退出误释放整个组。
- `verification-runtime.test.ts` 已通过两名执行者、两名独立验收者的真实 Pi 工具循环：A 文件缺项后只返工 A，B 验收范围不足后只重查 B，两项均由协调者二次复核。A 写入两次、B 写入一次，两名验收者各检查两次；工作项与团队收口，Task 仍待用户接受。发现重复展开全部派发/历史资料会耗尽协调上下文，已将验收概览改为当前摘要，并提供完整报告分页读取。
- 较早一次全量 Host 回归通过 289 项；之后仍有上述验收概览和界面调整，最终回归需重新绑定最终候选。客户端全量回归发现新增模型需明确启用，错误提示已区分未连接与已停用；真实 Host 客户端路径已复验通过一次，后续全量及视觉复验仍待完成。

剩余收口：协调者创建权在旧公开团队入口的约束、部分派发失败与容量等待、最终额度来源复验、遗留界面的完整组合走查与物理拖放、旧 Swift 界面和打包退役，以及最终全量回归、独立审查和人工验收。前述历史待完成列表按本节的较新证据理解，不表示已交付或已提交。

</details>

## 分批交付与验收记录

按依赖和可独立验证能力推进；后一批未提交实现不能作为前一批通过检查的前提。每批以实际提交为边界，保留未完成改动，提交前独立审查；本地提交不等于人工验收、主干合并或发布。

| 批次 | 能力边界 | 本批状态 |
|---|---|---|
| 1 | 文件树、多标签、Markdown/HTML 编辑、隔离 HTML 预览、Git 状态和逐文件差异、精确引用、产物正文入口 | 独立候选自动检查与代码审查通过；尚未由 507 人工验收 |
| 2 | 独立智能体执行进程、配额回退、成员派发/验收、持续消息、历史续接与运行依据 | 独立候选自动检查与故障复审通过；人工验收未完成 |
| 3 | 已有项目目录维护 | 独立候选自动检查与复审通过；人工验收未完成 |
| 4 | Pi SDK 三核心包升级至 0.85.1 | 独立候选自动检查与审查通过；人工验收未完成 |
| 5 | 旧客户端退役与候选打包承接 | 自动检查、实际构建/切换/回退与独立审查通过；507 人工验收未完成 |
| 6 | 真实协作组合与运行中来源显示 | 真实组合到用户验收边界；来源修复回归通过；物理拖拽及 507 人工验收待完成 |

### 批次 1：文件与 Git 工作台

范围来自前一版迁移遗留；不依赖后续自适应协作运行改造。Host 从已登记任务、项目或产物推导访问目录，客户端不得提交任意本机根目录。Markdown/HTML 保存检查原始内容版本，外部修改时提示冲突；运行中的写入与文件保存不能重叠占用目录。原生文件辅助程序固定目录句柄并拒绝符号链接、越界和敏感路径；Git 状态与差异使用同一限制，单文件产物不扩权到父目录。

HTML 在独立的沙盒视图内呈现当前编辑内容，本机资源仍按登记来源读取，联网资源默认阻止并需当次显式允许。文件与 Git 差异引用保留文件路径、行或新旧侧以及差异版本；切换任务保留编辑标签，关闭和退出遇到未保存内容时要求保存、放弃或继续编辑。

验证入口：`host/npm test` 覆盖文件竞态、越界与敏感路径、单文件产物、Git 范围及保存/运行互斥；`client/npm test` 覆盖真实 Host 的编辑、外部冲突、覆盖、行引用、标签及退出保护；`client/npm run test:preview` 使用真实 Electron 覆盖 HTML 隔离；布局与共享输入规则按客户端标准命令检查。2026-09-07 在仅含本批改动、从 `7956d709` 建立并独立安装依赖的验证工作树通过 Host 251 项、客户端主进程 7 项与界面/工作台 31 项、40 种布局、208 种输入状态、真实 Electron HTML 隔离预览和 Host 握手检查。本机未签名候选构建成功，打包内的 Host 模块与原生文件辅助程序完成实际读写；只作本机验证，不作发布物。独立审查提出的 Git 范围和启动/关闭阶段保存互斥问题已修复并复审通过。507 人工验收未完成；不把早先混合工作树的检查当成本批证据。

### 批次 2：任务内协作运行与持续对话

本批把独立进程、辅助进程、模型供给、派发/验收、持续消息、来源回执与原生历史续接作为完整路径交付；上下文与技能/命令交互共同验证。项目目录维护和旧 Swift 客户端退役仍在后续批次，未借本批混入。2026-09-07 在从 `24149a1` 建立、独立安装依赖且只含本批改动的工作树完成验证：Node 22.22.3，Host `npm test` 302/302、客户端主进程 7/7 与界面/工作台 35/35、布局 40/40、输入状态 236/236，差异格式与 69 个相对链接检查通过。独立审查要求修正的普通/conflict 关闭保护已用真实 SQLite 和实际 Adapter 文件变更复验通过。原实施树只接收经核对相同 Git tree 的本批暂存快照；本地提交不代表人工验收、主干合并或发布。

运行握手区分进程就绪、已接收本轮工作、实际运行、空闲和退出。接收确认有独立期限，不给正常长任务设置总时限；接收失败先终止所属进程并等到真实退出，再释放占用。保存失败保留未确认运行和目录声明，即使关闭会话也不能伪装干净释放；重新连接后恢复为中断并保留文件，不自动重做。

原生运行在结果落库前处于保存中。报告、来源消息终态和通知在同一 Product Store 事务内写入，各事件仍有唯一递增修订号；查询不得看到未提交状态。来源消息的完成引用与通知所指成员结果的引用分别保存，同一运行重复完成不重复通知。旧候选已提交结果但漏通知的记录在重启时补齐一次，修复后的通知保持暂停。

### Wemux 失败场景核对（2026-09-07）

借鉴固定提交 `179e6aa5c6277e4a73c2773028114eeb59fedc86` 的测试所防范的错误假设和故障注入方法。这些测试不证明场景曾在线上发生。D Code 保持本机 Host 管理产品事实、独立智能体进程执行；不引入跨机器 worker、网络调度、远程控制面或多节点部署。

| 场景与参考 | 核对结论及本批处理 | D Code 验证入口与边界 |
|---|---|---|
| [停止后继续保留占用](https://github.com/wemux-ai/wemux/blob/179e6aa5c6277e4a73c2773028114eeb59fedc86/apps/worker/src/runtime/message-handler/prompt-queue.test.ts) | 已有充分覆盖的真实进程/目录保护继续沿用，不机械补同名测试 | `host/test/auxiliary-process.test.ts`、`auxiliary-runtime.test.ts` 实际启动进程组，核对 PID 存活、退出及无关进程继续；Host 保存被拒/恢复可写分别验证。 |
| [就绪后不再响应工作](https://github.com/wemux-ai/wemux/blob/179e6aa5c6277e4a73c2773028114eeb59fedc86/apps/worker/src/execution/runtime-launch-smoke.test.ts#L303) | 初查发现缺少 run 接收确认；新增 accepted/start 握手和独立接收期限，未照搬参考超时值 | `host/test/process-agent.test.ts` 启动真实 ready-only 程序，确认实际退出后才释放，推理调用为零，无关智能体仍可运行。 |
| [旧回答不能替代本轮输出](https://github.com/wemux-ai/wemux/blob/179e6aa5c6277e4a73c2773028114eeb59fedc86/apps/worker/src/execution/pi-runner.test.ts#L34) | 移除无本轮输入锚点时的历史 leaf 回退；流式接管使用消息身份及输入边界，不按全局相同文本去重 | `host/test/completion-delivery.test.ts` 先保留旧报告，再制造空输出、失败及停止，核对本轮无旧回复/报告引用；`client/test/conversation-origins.test.mjs` 覆盖 steering 前后消息与工具的流式到落盘归属。 |
| [恢复保留已有成果与缓存](https://github.com/wemux-ai/wemux/blob/179e6aa5c6277e4a73c2773028114eeb59fedc86/apps/worker/src/runtime/local-worktree.test.ts) | 原有真实工作树恢复测试补充恢复前文件与依赖缓存断言；保持拒绝篡改，不用重建掩盖失败 | `host/test/dispatch-recovery.test.ts` 在 ready 回执丢失的真实工作树中预放成果和 `node_modules/.cache`，恢复及 Host 重启后逐一读回，源项目正文不变。远程克隆与跨机器分支准备不适用。 |
| [完成、回传、验收和用户接受分离](https://github.com/wemux-ai/wemux/blob/179e6aa5c6277e4a73c2773028114eeb59fedc86/apps/server/src/services/workspace-session-completion-notifier.test.ts) | 沿用多成员真实工具验收；补足通知的固定交办者、Session Run 和 Report 身份，通知消费引用与成员结果引用不混用 | `host/test/verification-runtime.test.ts` 两执行者/两验收者完成局部返工与重查后 Task 仍 active；`completion-delivery.test.ts` 核对通知对象、准确运行/报告、重复完成与重启不产生重复通知。 |
| [保存成功后才广播](https://github.com/wemux-ai/wemux/blob/179e6aa5c6277e4a73c2773028114eeb59fedc86/apps/server/src/cluster/task-sync.test.ts) | 最新候选复现“先完成再通知”缺口，改为事务原子交付；保存失败不发可靠完成信号，不开始该运行的后续工作 | `host/test/completion-delivery.test.ts` 在真实事务提交前阻塞，用第二个 SQLite 连接确认未提交结果不可见；用 SQLite trigger 拒绝通知写入，断言整个交付回滚、关闭不解除保护、重启保留文件而不重跑；旧候选通知缺口补齐一次并暂停。 |

上述模型响应均为受控测试响应，进程、文件、Product Store 与重启路径为实际执行；它们不证明真实供应商额度查询、完整窗口操作或 507 人工验收已通过。用户提供的局部方法抽取证据只用于定位，未冒充完整 Host 验收。

### 批次 3：项目名称与目录维护

范围：编辑项目名称、更换唯一关联目录、可选移动项目文件、私有 Runtime 会话续接与中断恢复。Task、D Code Session 与原提交保留原身份；原 Adapter 字节不覆盖，变更使用有摘要的新私有副本。文件移动限同盘空目标，拒绝嵌套项目、关联 Git 工作树和运行数据/当前应用边界；不搬运未知状态的数据。

prepared/unknown 计划中的源、目标路径进入持续写入保护，其他作用域、旧会话与重启不能绕过；恢复确认后才释放。项目登记在准备事务中重新验证，避免检查后新出现的嵌套项目被一起搬走。编辑缓冲同时按项目归属和实际移动路径检查；从 User Scope 打开的同一文件也受保护，只有关联变化时无关缓冲保持原状。

验证入口为 `host/test/project-maintenance.test.ts`：实际目录交换、私有会话续接、原文件读回、未确认交换与 Host 重启、跨作用域写入拒绝/恢复、准备期间新增项目及系统路径别名保护；`client/test/ui-flow.test.mjs` 使用真实 App 和 Host 完成项目重命名、勾选移动与文件读回，系统目录选择响应受控替换；`workspace-files-flow.test.mjs` 核对跨作用域未保存缓冲和标签失效。自动检查、独立审查与原生窗口/507 人工验收分别成立。

第三批验证记录（2026-09-07）：从 `d536846` 建立独立工作树、独立安装依赖，仅含本批改动；Host `npm test` 305/305，客户端主进程 7/7 与界面/工作台 35/35，共享输入 240/240，构建、差异格式和相对链接检查通过。独立审查已复核 unknown 目录保护、晚登记项目与编辑缓冲边界。原生窗口完整路径与 507 人工验收尚未完成；本批只作本地源码提交，不推送、合并或发布。

### 批次 4：Pi SDK 0.85.1

507 指定三个核心包 `@earendil-works/pi-agent-core`、`@earendil-works/pi-ai`、`@earendil-works/pi-coding-agent` 统一精确升级为 0.85.1。[0.85.1 发布说明](https://github.com/earendil-works/pi/releases/tag/v0.85.1)确认修复 0.85.0 误发布实验代码导致的导入失败；[0.85.0 修复项](https://github.com/earendil-works/pi/releases/tag/v0.85.0)包含工具 cwd、取消与压缩/分叉边界。[AI 层变更](https://github.com/earendil-works/pi/blob/v0.85.1/packages/ai/CHANGELOG.md)中的新流式片段编码与外部条目恢复仅作后续候选；本批不替换历史、存储或 Harness，不引入终端界面和实验服务端/客户端。

本批基于 `c1f5f31` 的独立验证树，原工作树的其他文档与收尾改动保留。必要适配只有工具的 `executionMode` 跨进程保留，避免 SDK 已声明的顺序执行策略丢失；模型目录刷新测试改用晚于当前内置目录的受控时间戳，保留“新目录可选且可恢复”的断言。历史验证记录仍保留当时 SDK 版本。

新增 `host/test/sdk-compatibility.test.ts` 核对：七种内置工具在构建目录与当前 ctx.cwd 不同时实际读写/搜索/执行；两个工具及分片参数执行完毕后才消费连续引导；停止不执行后续工具且保留未消费原文；手动压缩取消与在途复制拒绝；完成压缩后通过 D Code 复制并续接保留摘要边界；顺序工具跨独立进程；GPT-6 Astra Codex SSE 末尾没有空行也能完整结束。

真实供应商验证使用已授权的智谱 GLM-5.3，经 SDK 只读凭据接口访问，原认证文件不改动、凭据不复制进验证目录。隔离 Host 与独立执行进程完成 read/write，三次真实请求返回 200，low 思考级别与调用记录一致；实际 Electron 窗口读取结果、改为 high 后再次发起只读调用，两个真实请求携带 high，重启后对话和选择恢复。真实 GPT-6 Astra、Claude 专属传输及全部供应商组合未逐一实测，不将受控协议测试冒充这些实测。

第四批验证记录（2026-09-07）：独立安装依赖后，Host `npm test` 312/312、客户端主进程 7/7 与界面/工作台 35/35、客户端构建、差异格式与相对链接检查通过；独立审查通过。实际 Electron 窗口和五次智谱请求已验证上述路径，未进行 GPT-6 Astra/Claude 的真实账户组合测试。SDK 升级不代表整体 0.0.31 人工验收或发布。


### 批次 5：旧客户端退役与候选打包承接

退役旧 SwiftUI/AppKit 界面、状态、Host 桥、SwiftPM 测试、RelaunchHelper 和 `app/build.sh`，共 118 个仅属旧客户端的已跟踪文件；退役前源码可在 Git 基线 `18e12df` 查阅。保留 `app/Resources/` 品牌母版、Host 原生文件辅助程序、旧偏好/产品数据导入、当前自进化构建和候选切换合同。品牌导出只生成当前客户端与系统图标，不再写退役目录。

独立审查发现并补齐旧打包链的许可证承接：Electron 候选携带 D Code 声明、运行时原有许可证、Host 与客户端生产依赖清单及许可证正文；精确锁定依赖中缺少正文的已审计包保留归属与完整条款，未知声明或未经审计的缺失正文阻止构建。当前候选不再声称额外携带 Node 22.22.3 或 ViewInspector。

实际产物验证：包内 Host 握手确认 Pi 0.85.1，包内 `dcode-files` 完成真实读写；通过生产 `maintenance.start` 构建新的完整应用，再由生产 `switchApplication` 停机并切换。新候选的实际窗口恢复原任务和未发送草稿；点击“回滚上一构建”后，原应用窗口仍保留相同任务/草稿，两条 Product Store 回执均为 `rolled_back`，没有代替 507 点击人工接受。品牌导出在临时复制中完成，三个输出与当前品牌资源字节一致且不创建旧源码目录。独立审查通过。

本批验证树从 `18e12df` 建立并独立安装依赖；Host 312/312、客户端主进程 7/7 与界面/工作台 36/36 通过。新增许可证故障检查实际制造缺失正文和未知声明，并核对成功时正文被原样归档。本机未签名候选构建通过，产物包含 Host 165 个与客户端 171 个唯一生产包记录、Electron 原始 notices、Pi 0.85.1 及原生文件辅助程序。构建、独立审查、窗口验收和发布仍分别成立；不推送、不合并、不发布。

### 最终窗口走查与仍待完成的验收

- 在当前 `18e12df` 实施树的 Electron 窗口中，真实智谱调用完成“编辑旧消息并继续”和“从旧助手节点继续”；原路径可回看，原文件未重做。
- 使用真实系统目录选择窗口创建验证项目、保存对话、改名并移动到同盘空目录；窗口保留原对话并显示新目录，原文件字节读回一致。迁移后真实模型通过 read 读到新目录中的 `keep.md`，返回原文，未改动文件。
- Finder 跨应用拖入已尝试，自动化操作未在 D Code 形成附件；不能将此结果判断为产品通过或产品故障。文件选择、粘贴及受控拖入有自动回归，跨应用物理拖入仍需可观测的实际操作结果。
- 完整灵感→任务→多成员→定向补充→独立验收返工→回收续接组合已完成下面记录的真实客户端与模型走查，并保留在用户验收边界；507 人工验收未完成。

来自工作台原型任务的 UI/UX 交接作为后续设计输入保留：整页新任务入口、仅新任务页使用指定 ThreeUI 原版背景、双主题及从操作按钮中心扩散是已确认方向；八项建议和三批建议顺序不自动成为当前交付承诺。本轮不混入这些呈现层改造，最新原型保留在主工作区 `doc/20-产品与交互/原型/dcode-workbench-v2-prototype.html` 及 `dcode-workbench-v2-source/`，不清理其未跟踪文件，也不用隔离树中的老副本覆盖它们。


### 批次 6：真实协作组合与运行中来源修复

2026-09-07 在 `6d32079` 隔离实现的真实 Electron 窗口中，从画布创建文字灵感 r1、引用为新任务、向协调者提交有界目标；实际 GLM-5.3 自主派发两名执行者和两名独立验收者。A 首版缺少 SOURCE，被真实 read 证据判 fail，经协调者 rework 后仅 edit 一次补齐并复验通过。B 只 write 一次；用户从主对话提及 B、进入其子对话补充要求后，B 仅回复，新要求回流协调者。协调者对 B 最新报告追加只读复验，未重写成果。四项工作完成，任务仍 active，并在实际窗口显示用户验收请求；没有替 507 接受任务。

本轮共 63 次真实 GLM HTTP 200 调用。每名成员的有序链先遇到受控的 MiniMax-M3 1% 响应并跳过，再使用真实额度查询确认可用的 GLM-5.3；不把受控值当作 MiniMax 真实余额。四名成员拥有不同主进程；A 返工、B 主对话提及及子对话续接记录了原 PID 退出和新执行 PID，延续原成员/会话身份。本轮任务文件的工具写入仅为 a.md、b.md 的两次 write 与 A 的一次 edit。原文、报告、验收及复核身份从 Product Store 读回核对。此前隔离环境把 Data Root 放在测试 Home 之外，灵感引用被保护拒绝；修正测试目录关系后再进行本轮，不以此放宽产品路径约束。

走查确认的呈现缺口：协调者收到后台结果或定向更新，正在模型调用/等待用户时，输入尚未与最终助手条目一起建立持久来源映射，通知会暂时被标作“你的消息”。按当前 ActiveRun 的原生输入 ID 与已确认 Adapter 输入 ID 提供只读来源投影，不等待本轮答案，也不提前保存结果或推动完成状态。没有按通知文字前缀过滤，因此真人输入相同文字仍保留用户身份；完成后的持久映射保持单一来源。

`host/test/completion-delivery.test.ts` 新回归实际阻塞协调者 Provider 调用，断言此时没有助手结果、确有已写入 Adapter 的输入，来源已经指向准确协作消息；旧实现该断言失败，修复后通过，并继续验证落盘后不重复及同文真人输入。独立验证树基于 `6d32079`，Host 313/313、客户端 7/7 与 36/36、构建与独立代码审查通过。修复后的实际 Electron 窗口复验通过：在 Provider 被阻塞、协调者尚无回答时，主对话只显示一条真实用户消息和“进展更新”，不再暴露生成通知为用户消息；点击停止后该段仍为进展更新并显示“已停止”，原用户回答与成员成果保留。跨应用物理拖拽没有取得可判定结果，继续列为待人工验收，整个 0.0.31 尚未由 507 验收或发布。
