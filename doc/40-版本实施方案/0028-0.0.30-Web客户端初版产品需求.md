# D Code 0.0.30 Web 客户端初版产品需求

状态：基础功能已通过 507 人工验收（2026-09-07），技术路线由 ADR 0044 接受。验收范围是当前 Web 工作台、灵感与本轮修复；Swift 拆除、未迁移内容面和正式发布仍分别处理。下方早期候选状态保留为过程记录，当前结论以本次验收收口为准。

本 PRD 初始范围是 Web 客户端的**初版画面与壳**：以 Electron + React 壳消费既有 `host/` 与 Protocol v1，落地已拍板的主页面三区布局，验证打包链与动效基线。后续 507 授权的设置、附件和灵感接入按下方补验记录扩展；这些新增核心合同不得继续被描述为已有能力的纯呈现迁移。

## Problem Statement（问题陈述）

`0.0.29` 基线的 SwiftUI 客户端迭代速度无法承载「原型逐区拍板后快速落地」的节奏：每次界面合同变更都要付出原生侧重验成本，507 已明确不可接受。同时主页面布局合同（三区、浮动梗概、详情侧栏、稳定几何）已于 2026-09-05 拍板但尚无消费它的客户端。本版用已实证的 Web 技术栈建立新客户端主路径，验证从窗口、核心拉起到真实数据上屏、打包候选的完整链路。

## Solution（解决方案）

### 1. Electron 壳（平台壳）

- 主进程只承担 [0003 议题二](../10-架构与运行/0003-Web客户端技术栈.md) 的七项职责：窗口 / 菜单 / 快捷键、系统通知（设置可开关）、文件选择对话框、系统权限出面、Host 定位 / 启动 / 健康检查 / 受控重启、事件转发、检查更新占位。
- 判断口诀不变：门面职责必须「换个产品也通用」。
- `0.0.30` 只建一个系统窗口；梗概浮卡与详情侧栏均为主窗口内部布局。

### 2. Host 接入（议题一 / 议题六）

- Host 保持独立子进程：主进程以 Electron 二进制 Node 模式（`ELECTRON_RUN_AS_NODE=1`）执行 `host/dist` 入口，环境按现有 Swift `HostLocator` 合同平移（`PI_CODING_AGENT_DIR`、`NO_COLOR`、`NODE_OPTIONS` 告警抑制）；不再单独打包 Node 运行时。
- 传输为 stdio 管道直迁，Protocol v1 零改动；首版消费 `host.hello` 握手、`foundation.snapshot` 查询与既有事件信封。
- 电话线预铺三条约束随首版代码成立：协议编解码与传输解耦（纯 TS 协议客户端模块）、渲染层永不直接触碰传输（一律经主进程桥接的版本化 IPC）、升级路径（本地服务 + token + 就绪探测）只留接口不实现。

### 3. 主页面三区布局（消费既有拍板）

- **导航区**：身份与轻工具、创建动作、最近工作、项目（含 Task 与已启动子会话）、独立任务、底部设置——按[主页面 UI/UX 设计](../20-产品与交互/0002-D-Code-主页面-UI-UX-设计.md)顺序与文案映射。
- **工作区**：默认内容为当前 Task 的任务对话（Coordination Session）；Workspace Tab 合同进入但 `0.0.30` 只读。
- **信息区两态**：梗概 = 右上浮卡，关闭时正文、空态与输入区居中，打开后共同向左让位并按可用宽度调整；内容合并进度追踪与 Agent Team 追踪。详情 = 侧边整幅子栏，宽窗并列后读列在剩余空间居中，窄窗覆盖呈现；具体对象详情取代梗概，关闭后恢复。
- **稳定几何规则**是验收硬项：同一布局内，选中、悬停与运行状态刷新不得改变既有行的行高、列宽、列位置或滚动位置；当前态只用颜色、描边与图标表达。显式打开或关闭梗概、详情按上述结构切换规则调整布局。

### 4. 呈现层（议题三 / 议题七）

- Vite + React + TypeScript；zustand 管画面小账、swr 消费 Host 投影，配自动检查禁止核心数据类型进入 zustand。
- Tailwind v4 + Radix/shadcn + Motion 三件套；动效参数（浮层、悬停、转场）在本版定基线并写入设计系统回写。
- 只读内容用 Shiki 上色；不引入编辑引擎与终端部件。

### 5. Swift 客户端替换与拆除策略（2026-09-05 507 指令）

- **按面替换、验证即删**：0.0.30 开发中，每个界面被 Web 实现并通过对应验收后，同批删除 Swift 侧对应实现；验收时仓库不得残留同一界面的双实现。
- 回退依赖 git 基线（`714a555` 及其后提交），不依赖并行代码；`app/build.sh` 打包链随最后一批 Swift 界面拆除一并退役，由 electron-builder 候选链接管。
- `host/`、`Package.swift` 之外的 Swift 侧共享合同（如 HostLocator 语义）以移植而非保留的方式进入 Electron 主进程。

### 6. 打包与候选

- electron-builder 产出可双击的本地 `.app` 候选；候选隔离、显式重启与回滚沿用 ADR 0022 精神，签名 / 公证要求沿既有发布纪律，完整更新链路归发布专项（本版只有检查更新占位）。

## User Stories（用户故事）

- 作为 507，我双击候选 `.app`，看到真实 `~/.dcode/` 里的项目、任务与会话，而不是模拟数据。
- 我在导航区切换任务，工作区进入任务对话，切换不停止任何后台 Runtime。
- 我打开梗概浮卡，能同时看到任务进度和团队成员（谁在跑、谁在等我）；点开一个对象，详情侧栏展开并挤压任务区，关闭后恢复。
- 我重启 App，工作台选择、梗概分区与阅读位置从 Product Store 恢复。
- 我全选、悬停、展开任何行，已有内容纹丝不动。

## Confirmed Constraints（已确认约束）

- 层级语义服从 [ADR 0042](../决策档案/0042-客户端呈现与-D-Code-Host-写入边界.md) / [ADR 0044](../决策档案/0044-Web客户端与桌面壳技术栈边界.md)：渲染层不写 Product Store、Project Directory 或凭据；凭据红线与存储三档落点服从 0003 议题四。
- 保持 Runtime Supervisor、Prompt / Tools 与产品所有权；只为下方明确的恢复范围补充原生数据接口。无派发、无多目录、无工作区文件编辑器、无终端、无电话线启用。
- macOS-only；不做跨平台抽象。
- 界面文案简体中文优先，术语服从根 GLOSSARY 与主页面文案映射。

## Acceptance Criteria（验收标准）

1. 可双击 `.app` 候选启动、退出、重开；退出走 Host 优雅停机（等 Run 收口，超时强杀）。
2. 主进程以 Node 模式拉起 Host 并完成 `host.hello` 握手；Host 崩溃时窗口存活并呈现恢复入口，可单独重启 Host。
3. 导航区 / 工作区 / 梗概浮卡 / 详情侧栏按第 3 节合同呈现真实 Product Store 数据（`foundation.snapshot` 及既有查询）。
4. 稳定几何规则通过 507 走查：同一布局内选中、悬停与内容刷新零位移；梗概关闭时居中、打开时让位，正文与输入区始终对齐。Motion 动效基线（浮层、悬停、转场参数）成立并回写设计系统。
5. `host/` 全量测试保持绿；client 侧新增自动化（协议客户端单测 + 壳冒烟：拉起 Host → 握手 → 查询 → 退出，不开窗口可跑）。
6. 通知可开关；检查更新为占位。
7. Swift 按面拆除清单执行完毕：已验证面无双实现残留，仓库体积不因并行客户端膨胀。
8. 507 人工试玩确认后，ADR 0044 转 Accepted。

## Eval Scope（评测范围）

- 从打开到看见真实任务数据的链路是否诚实（无模拟数据兜底）。
- 梗概「进度 + 团队」与详情挤压式侧栏是否符合两态心智；几何稳定性是否经得起连续操作。
- 壳 / 核心 / 画面三层职责是否守住（画面无产品写入路径）。

## Out of Scope（非目标）

- 任务内自适应协作（0.0.31）、工作台 UI/UX 升级（0.0.32）、多目录项目（后续待排期）；电话线启用与多端接入。
- 编辑缓冲区、终端、命令面板、附件、图片发送等既有能力的完整迁移——未被本版验收清单覆盖的界面保留在 Swift 侧直至被替换。
- 自动更新链路、签名分发、跨平台。

## Verification Record（验证记录）

2026-09-05 对 `main@1d4fd48` 的只读审查确认：React 界面不在类型检查范围，独立检查得到 13 条诊断；新任务、流式事件、运行控制、草稿隔离、搜索、导入、打包路径存在缺口。此前构建、协议 7/7、Host 226/226 通过，只能证明各自覆盖范围，不能证明工作台可用。

507 随后授权修复。当前未提交候选：

- 恢复原 Swift 品牌资产，新增 React 全目录类型检查与真实 Host 隔离交互回归。
- 复用任务创建、会话草稿与工作台选择接口；为既定恢复目标补充 `taskDraft.set`、`clientPreferences.get/set`，保存未创建任务的文字草稿、阅读位置和通知偏好。Protocol 信封、Runtime 和产品所有权不变；新增方法为本次修复所需的加法扩展，原“协议零改动”描述仅适用于最初骨架。
- 新建任务、发送、增量消息、运行失败、模型与停止、切换、搜索、Pi 导入、信息概览、系统菜单和候选打包按真实合同修复。
- 退出前保存文字草稿与阅读位置；失败时留在工作台或由用户选择退出。图片附件只按草稿在内存中隔离，退出提示，不声称跨重启恢复。
- 第一轮修复曾撤下会话操作且将设置缩减为模型页；507 明确拒绝该退化。后续按原 Swift 设置和原 Web 已有功能恢复，见下方补验记录。通用文件查看器与编辑重走仍未迁移。

验证入口：`npm --prefix host test`、`npm --prefix client test`、`npm --prefix client run build`、在 `client/` 执行 `npm run smoke:host` 与 `npm run dist`。UI 测试只替换模型网络响应，实际执行 React → PiHost → Product Store → Runtime → 事件 → React 链路，覆盖首次创建/流式、停止、独立草稿、重启恢复以及完整 App 的搜索与创建入口。其通过不代表真实供应商认证或视觉人工验收。

Swift 面未删除，ADR 0044 未转 Accepted；本次修复不构成提交、发布或人工验收证据。最终命令结果随本次交付收口记录。

第一轮自动验证（未获 507 验收，2026-09-05，`main@1d4fd48` 上未提交工作区）：Host `227/227`，client 协议 `7/7` + 呈现/隔离交互 `7/7`，完整类型检查与构建通过。首次 Host 并行回归的两项时序测试失败，单独 `11/11` 复验及随后完整 `227/227` 通过，断言未修改。独立审查发现全部修复后复验收口。

隔离交互还复现并修复了停止后重开续接问题：固定 ensure 请求身份会回放终态协调者；改为每次显式 prompt 的稳定请求身份，并在无在途 Run / 活动 Team 时关闭旧 Runtime，创建新 Agent Run 所有者，旧终态证据保留。已有 Runtime 的模型切换核对真实 Provider 请求使用 `model-b`，不是只验证下拉显示。

`npm --prefix client run dist` 生成未签名本地候选，实际用包内可执行文件、包内 Host 和包内依赖启动，通过隔离 Product Store 读取与正常退出；日志无启动错误，原图标资源 SHA-256 与 Swift 资产一致。候选只是本机调试产物，不能作为正式发布物分发；真实 Provider 认证/付费请求、完整图片重启恢复、Swift 拆除及 507 人工验收仍未进行。

候选窗口走查：实际 Electron 包内运行核验任务切换、A 草稿不出现在 B、返回 A 恢复草稿、⌘N 新建草稿、⌘K 搜索并返回对应任务、任务概览按需展开。1440px 浅色与 800px 深色截图均已查看；系统菜单为 D Code 中文入口，启动/退出日志无错误。走查使用 `/tmp/dcode-review-1d4fd48/` 隔离数据，未发送真实供应商请求。507 人工验收仍待完成。


### 设置与原 Web 能力恢复补验

507 指出设置分类缺失及原 Web 能力被删后，恢复范围扩展为：完整设置导航及对应操作、任务重命名/复制/归档/恢复、运行统计、上下文路径、输入区思考强度与完整模型目录。原 Swift 品牌资产继续复用。前一轮自动检查及截图不能代替这些功能的恢复。

| 原有能力 | 本次接回方式 |
|---|---|
| 模型与供应商 | 全目录/认证态/启用范围/默认模型与思考强度；供应商原生编辑和显式接管旧来源，保留非敏感模型元数据和认证引用 |
| 本机资源、智能体档案 | 来源清单、技能与提示词启停、档案创建编辑启停；外部可执行扩展按现有核心策略限制 |
| 外观、工作台 | 主题、字号、栏位显示和共享宽度、恢复默认；旧 Swift 偏好单向继承，新设置优先 |
| 归档管理 | 原生任务归档/恢复；复制完整会话历史为新任务，保留来源关系；Pi 单向导入 |
| 自进化 | 检查、隔离构建、候选重启、原任务恢复、人工确认、回滚及事件记录 |
| 通知、诊断、关于 | 通知偏好与系统入口，存储健康与日志、空闲检查后的 Host 重启，原图标及版本信息 |
| 原 Web 消息体验 | Markdown/代码/Mermaid/图片、复制引用、时间戳、附件菜单、任务上下文、运行统计、模型和思考选择 |

验证使用隔离 Product Store 和模拟供应商网络响应，不写入真实 Pi 设置或认证文件。新增回归覆盖偏好继承与重启、原生供应商和模型实际调用、任务操作、完整设置入口、候选身份/恢复失败/回滚取消以及旧 Host 确认退出。构建、实际窗口走查和 507 人工验收分别记录；当前仍无提交、发布或 Swift 拆除。

恢复补验的最终自动检查：Host `230/230`；client 协议 `7/7`、呈现/真实 Host 交互/停机/候选切换 `13/13`，完整类型检查通过。期间发现团队收尾会先把停止回执记为未知，使原 `runtime-supervisor` 重放断言间歇失败；已在 Host 确认匹配 Runtime 停止后，允许该明确身份的停止回执收口成功，其他未知副作用不变，原断言未修改。最终完整回归通过。

实际设置窗口已核验全部分类，执行主题切换、共享栏宽、档案创建、无凭据供应商配置保存、通知开关、Host 重连、任务重命名及草稿输入。原图标在品牌区和关于页可见。第一次从设置构建候选时，通用凭据检测误把带 UUID 的完整候选路径识别为凭据，保存失败造成 Host 退出；已改为绝对路径及逐组件验证、保存失败可见反馈，并在设置页增加断线恢复入口。该失败保留为真实路径补验依据，不能算构建恢复通过。


最终打包路径补验（同日，隔离数据 `/tmp/dcode-restoration-acceptance/`）：设置页构建成功后，实际从 `/tmp/dcode-restoration-ready/mac-arm64/D Code.app` 切换到 `dist-candidate/web-eac17646-06e8-476f-a16c-da7cbd7043cd/mac-arm64/D Code.app`，新窗口显示原任务已恢复，原文字草稿一致；随后实际回滚，上一构建重开，原任务及草稿仍一致，两个回执均正确记为 `rolled_back`。进程核验显示每次交接后仅目标应用及其 Host 存活。人工验收按钮未点击。

切换补验还修复了 Electron 将 `app.asar` 当作虚拟目录、导致普通文件读取失败的问题；摘要读取在 Electron 内改用 `original-fs`，不修改全局 ASAR 设置。新增真实 Electron 子进程回归后，client 最终为协议 `7/7` + 交互/呈现/停机/候选 `14/14`。原品牌图像与 Swift 源文件逐字节一致。最终窗口另实际完成了复制任务 → 归档 → 从设置恢复的完整路径。

未覆盖/未迁移边界：真实供应商登录与付费请求、未发送图片跨重启、通用文件查看与编辑重走；自进化回执页显示本轮原生 Web 切换记录，旧 Swift 自进化记录仍保留在原实现。自动更新、签名分发、Swift 拆除及 507 人工验收均未执行。以上本机候选未提交、未推送、未发布。


### MiniMax / ZCode 对照与真实 GPT-6 验收（2026-09-06）

507 新增的验收条件：在 D Code 正常新建一个不属于项目的对话，选择 GPT-6，并完成真实回复。之前无连接的隔离截图及模拟供应商回归不满足此条件。

本轮先只读拆解本机 ZCode 3.11.2、MiniMax Code 3.0.68，并操作其新建、模型选择与设置页面；差异、代码入口与采用边界见[本机对照](../参考文件/ZCode-MiniMax-Code-本机对照-2026-09.md)。未将参考产品代码或品牌资产引入 D Code，也未照搬套餐、权限模式或“未选项目”伪项目语义。

实现修正：模型展示与输入共用 `workbench/useModels` 和 `ModelPicker`，显示已连接模型、搜索及真实刷新；无连接时给配置入口。标准档案名称和默认职责以中文呈现，用户自定义文本不被静默改写。新任务标题和输入区相邻，顶部显示任务归属与工作目录。原 Logo、富消息、附件、任务概览、团队、证据和自进化保留。

Pi 三个直接 SDK 包从 0.84.1 对齐本机 0.84.4，并接通官方在线模型目录。刷新后由 Host 同步产品目录，已有原生供应商保持产品所有权；删除来源不会被刷新重新导入。原生 Runtime 读取 D Code 自有模型缓存，Pi 配置不双写。运行中的模型控制通过持久化请求身份防止重复执行；晚加入的供应商可以被已有 Runtime 选择。SDK 0.84.4 复制扩展 UI 上下文时不再触发主题 getter 的异常，实际使用 TUI 主题仍显式阻止。

自动验证：Host `232/232`，client 协议 `7/7` + UI/呈现/停机/候选/分层检查 `16/16`，类型检查与打包通过。新增覆盖在线模型刷新、删除不复活、缓存恢复、保持 Pi 设置原字节、晚加入供应商、选择请求重放及中文呈现不改变自定义内容。

真实窗口验收：运行包内 App / Host，使用正常 `~/.dcode/` 和现有 Pi 连接；通过界面点击新建、选择“独立任务”、选择“GPT-6 Astra”，发送一条要求中文确认并计算 17 × 19 的消息。收到并保存回复“已收到这条消息，17 × 19 = 323。”，随后通过界面将标题设为“GPT-6 独立对话验收”。

- Task：`task-d0f4ad44-49ef-4649-a541-113c76d59735`，`scope_kind=user`，`project_id=NULL`，cwd 为用户 Home。
- Session Run：`session-run-1fac8742-c48b-48e9-a10b-695756a41622`，`status=completed`。
- Runtime Environment：`model_provider=openai-codex`，`model_id=gpt-6-astra`。
- Assistant Entry：`entry-6b5c00c8-4ee4-458e-b1d9-7ce0291b874f`，正文与窗口回复一致。

这是一条真实模型回复，未使用模拟 Provider，也未通过参考应用代发。正常数据中的验收对话予以保留。本轮未提交、未推送，未拆除 Swift；其余架构收口和完整迁移不由这一条对话验收代替。


### 阅读留白与对话导航（2026-09-06）

本节记录此前的固定左侧起点候选。阅读列规则已由下方“画布工具与概览开关布局补验”中的最新确认取代，对话导航能力继续保留。

507 提供同尺寸 ChatGPT / D Code 截图，指出工作区左侧留白明显过宽，且缺少对话导航。当前任务将会话阅读列从整区居中改为共享左侧起点，并恢复 Swift 已有的轮次导航语义；不改 Host、不调用付费模型、不移除现有消息或任务能力。

在 1520px 窗口、280px 导航栏的布局中，正文左侧间距由约 280px 收至 92px，正文最大宽度由 680px 调整至 760px。正文、输入区与回到最新按钮对齐；宽窄视口和详情栏约束由同一组几何变量控制，概览开关不会移动正文。

新增独立的 Transcript、ConversationRail、轮次投影与滚动控制模块。轮次使用用户消息 ID，重复问题仍可分别定位。验证覆盖鼠标点击后继续键盘操作、首尾定位、手动滚动高亮，以及跳转位置被末尾限制时继续流式输出也不抢走阅读位置。

验证：client 协议 `7/7` + 交互/呈现等 `21/21`；类型检查与构建通过。`npm --prefix client run test:layout` 使用构建后的实际 CSS 和隐藏的独立 Chromium 窗口，对 800/1024/1200/1440/1520px、侧栏开关、概览开关和 340/600px 详情栏检查 20 组布局，全部通过。此检查与实际 App 走查分别成立。

实际 App 走查使用 `/tmp/dcode-rail-acceptance/` 中六轮长对话样本：点击第三轮、键盘进入第四轮、Home 回首轮、End 到末轮及手动滚动时高亮变化均已核验；概览关闭后正文和输入区维持位置。样本单独导入隔离 Store，没有污染正常对话。正常 Store 中此前 GPT-6 独立对话与回复继续保留。


### 执行过程、输入区与附件保留（2026-09-06）

507 指出执行过程展开为空、输入区出现默认蓝框/resize 手柄、拖入附件无反馈，并补充附件副本和预览要求。本轮按本机 ZCode 实际展开记录对齐：每轮默认一行实时进展，展开呈现完整可展示记录，最终回答单独阅读；不把空思考字段当成内容。输入区自动增高，焦点反馈留在外框。

新增已确认合同：选择、粘贴和拖入的文件由核心复制到 `~/.dcode/tmp/attachments/`，未发送副本保留 24 小时，提交后保留 30 天；草稿和已发送消息均有附件预览入口。图片显示缩略图并可放大；文件显示类型和文件名，并使用 macOS 快速查看。副本到期只清文件，历史引用保留“已过期”状态；已写入会话或工具记录的内容不按此周期删除。

草稿登记、发送晋升、保留期与清理由 Host/Product Store 负责，壳只按核心确认的 ID 路径调用预览。沿用 Schema 2，凭据与不可信路径校验继续成立。执行过程的投影、流式合并与附件显示分别位于 workbench 逻辑及纯展示组件，未更换 Logo 或删除原有设置、导航与任务功能。

验证记录：`main@1d4fd48` 上的未提交修复候选，Host `238/238`，client 协议 `7/7` + 交互/呈现等 `27/27`，实际 CSS 几何与输入焦点/resize 检查 `20/20`。独立审查发现的路径误报、凭据文件漏检、失败副本占用与缓存故障传播均已修正并复验。

本机界面路径使用 `/tmp/dcode-process-acceptance/.dcode` 隔离 Store：系统文件选择器同时加入 PNG 和 Markdown，草稿实际显示缩略图、类型与保留提示；将原件改名后，图片放大和文件快速查看仍成功。关闭并重启后，原文和附件草稿恢复，截图为 `/tmp/dcode-process-acceptance/managed-draft.png`。随后通过界面发送，本地隔离 Provider 驱动 Pi 实际读取受管 Markdown 副本，执行过程显示对应工具路径和完整返回，最终回复落入会话，发送后的文件卡仍可预览。`managed-proof.json` 记录两个副本存在、保留期均为提交后 30 天，提交原文未被生成的路径改写。

拖放事件与选择/粘贴共用受管导入路径，组件回归覆盖混合拖入、移除、预览和任务切换归属。CUA 的 Finder 跨窗口物理拖放未可靠完成，不能冒充该动作已人工通过；系统文件选择及其后的保存、恢复、发送和预览是独立实测通过的路径。新增链未消耗真实模型额度，此前正常 GPT-6 验收记录保留。

最终本机未签名包：`/tmp/dcode-attachments-final/mac-arm64/D Code.app`，构建及打包成功。已关闭旧候选并使用正常 `~/.dcode` 打开新版，原“GPT-6 独立对话验收”及回复仍在；展开原先空白的过程入口现明确提示本次没有可展示记录。未提交、未推送、未拆 Swift，人工验收权仍归 507。

### 全部输入焦点与灵感接入（2026-09-06）

507 再次指出全局搜索的蓝色矩形焦点框，并要求将规则写入 AGENTS.md、检查全部输入栏、补回灵感入口。蓝框来自共享 `focus-visible` 强调色规则，前次仅在 Composer 局部覆盖；旧的隐藏窗口测试没有强制激活焦点伪状态，因此没有发现其他输入面的问题。本轮改为共享中性焦点色与组合输入容器反馈，保留键盘可见性、文本选择和复选框的真实选中语义；所有多行编辑器禁止浏览器手动缩放手柄。

本机代码核验确认，“灵感”此前只在关系原型与 `/tmp/dcode-inspiration-canvas-prototype/` 中存在，没有正式 Swift、Host 或 Web 数据实现。因此本轮是把已有原型方向接入真实产品，不能声称恢复了并不存在的旧产品数据。左栏“搜索”下方提供入口，支持四种内容节点、搜索、编辑、平移缩放、框选、连线、成组、可逆归档与恢复；当前任务与消息正文也能保存为灵感。模拟 Agent 建议、自动知识加工和永久删除不在本次范围。

数据合同：Product Store 的 `knowledge.inspiration` 记录拥有 Markdown 正文、媒体身份、内容版本、布局与编辑草稿；内容和布局分别修改，陈旧编辑不能覆盖新版本。媒体长期保存在 `~/.dcode/knowledge/inspiration/media/`，不按对话附件的 1/30 天周期清理。任务引用使用原生 `global_knowledge`，绑定不可变 Markdown 版本并经真实 Prompt 来源加载；归档或后续编辑不改写已引用版本。导出只是受控快照，不是第二个可编辑权威。Schema 2、单写所有权、幂等请求、来源校验和凭据拒绝保持成立。

507 随后明确图片节点应自适应图片高宽。本轮完整显示原图比例，横图、长图、方图和小图等比缩放，节点随之改变宽高；连线、成组、框选、定位与“适应全部”读取实际节点边界，媒体加载按缩放后的边界与实际画布相交判断。连续新建不会把节点堆叠到同一位置。

验证绑定 `73970b6` 后本轮未提交代码：Host 全量 `243/243`；client 协议 `7/7` + 交互/呈现 `29/29`；类型检查与构建通过。自动扫描 `37` 处输入控件定义，深浅主题、聚焦/只读/禁用共 `192/192`；共享阅读几何 `20/20`；不同图片比例两主题 `8/8`。新增真实 React → Host → Store 回归覆盖同节点重复编辑、连续拖动的迟到响应、退出期间最后草稿保存、新节点避让、任务引用、归档恢复、重启草稿与放大后平移至画布边缘。

实际窗口使用 `/tmp/dcode-inspiration-window-U8e9PO/home/.dcode` 隔离 Store：搜索自动聚焦和 Tab、任务输入、模型目录搜索、供应商字段、档案多行编辑在深浅主题中核验；灵感文字保存、Markdown 快速查看、图片选择/缩略图/放大、选中后拖动、创建独立任务并引用、重启后节点与引用保留均已实测。本次不调用真实付费 Provider；构建与自动检查不代替 507 人工验收。完整本机候选输出在 `/tmp/dcode-inputs-inspiration-final/mac-arm64/D Code.app`，提交、推送与 Swift 拆除仍另行成立。

最终包已成功构建，包内主进程、预加载、当前页面/样式/脚本、四个核心变更模块与图标共 10 项字节核对一致。旧候选正常退出后，使用正常 `~/.dcode` 打开该完整包；“GPT-6 独立对话验收”及 `17 × 19 = 323` 的回复继续存在，搜索自动聚焦复验无蓝框，窗口留在新灵感入口。正常库不加入隔离验证样例。独立审查提出的草稿校验、编辑覆盖、上下文上限、空格键、退出保存、连续拖动及缩放边缘加载问题均已修复并复核；这仍是未提交、未推送的本机候选。

### 画布工具与概览开关布局补验（2026-09-06）

507 指出画布工具点击后缺少用途和后续动作说明。本轮保留紧凑工具条并增加中文名称、当前模式、快捷键与操作指导。平移仅改变视口，节点选择和内容位置保持原值；框选可从节点上起拖且支持 Shift 追加，多选后显示所选列表。连线提示起点、终点和保存结果；取消同一节点对时兼容旧双向记录。只有一条灵感时，连线与成组会明确提示先增加内容；已有多条但尚未选够时，成组引导框选。移动节点由节点自身持有指针事件，实际双击编辑不再被画布截走。

507 随后更新阅读布局要求：没有右侧任务概览时正文、空态和输入区居中，出现概览后才向左让位。此要求取代本节之前“开关不移动正文”的候选规则。布局直接跟随概览是否实际存在，不长期预留已关闭概览的空间；并列详情栏出现时在剩余空间居中，窄窗仍保留覆盖呈现与可读宽度。同步更新 DESIGN.md 与设计系统，正文、空态和回到最新按钮共用一组几何变量。

本轮新增真实 React → Host 回归覆盖平移/框选区别、Shift 选择、连线和旧双向取消、成组前置条件、临时空格平移与 Esc。窗口走查使用既有隔离 Store，实际完成平移、框选两条、成组、连接和双击编辑；只读核对确认平移只改变视口，节点位置保持不变，分组和连线确已进入 Store。布局检查扩展至空态/正文、宽窄窗、概览开关、详情与左栏开关共 `40/40`，关闭状态严格核对左右同心。未新增核心数据接口或调用真实付费 Provider。

最终客户端协议 `7/7`、交互/呈现 `29/29`，类型检查通过。实际空任务窗口核验概览关闭居中、打开左移；完整候选 `/tmp/dcode-canvas-tools-centered-final/mac-arm64/D Code.app` 构建完成并通过包内页面/脚本/样式、壳、核心模块与图标的 10 项字节比对。已关闭隔离窗口和旧候选，以正常 `~/.dcode` 打开此包；原 GPT-6 对话与回复保留，窗口留在关闭概览后的居中阅读状态。规则已同步主页面设计、设计系统、技术栈与本 PRD；独立复核未发现剩余阻塞。本轮未提交、未推送，507 人工验收与 `0.0.31` 实施另行成立。


### 基础功能人工验收收口（2026-09-07）

507 明确确认“暂且基础功能验收通过”，并授权 commit 与 push。接受当前已运行的基础工作台、完整设置、消息/附件、灵感保存与任务引用、画布工具及居中/概览让位交互，作为下一版本工作的可回退基线。运行候选为 `/tmp/dcode-canvas-tools-centered-final/mac-arm64/D Code.app`；具体机器验证记录见上文。

本次运行代码提交为：Host `5da40a4`、客户端 `584b779`。提交前复验 Host `243/243`、客户端协议 `7/7` 与交互/呈现 `29/29`；布局 `40/40`、输入状态 `192/192`、图片比例 `8/8` 沿用同一份已验收代码的检查记录。基础验收、提交推送与发行物发布分别成立。

这不是全量迁移或发行物验收：Swift 按面拆除未执行，通用文件/变更/Git 内容面与编辑重走尚未完整迁移；签名、公证和正式分发未进行。此前未可靠完成的 Finder 跨窗口物理拖放仍保留验证边界。下一版 0.0.31 的协作 PRD 随文档收口，当前没有启动其运行代码实施。

### 迁移遗留清单（2026-09-07）

507 在基础验收后要求明确剩余范围。以下以当前源码为证据，不把旧视图文件存在等同于当前主路径已经可用。Swift `RootView` 在取得 Foundation Snapshot 后进入 `TaskWorkbenchView`；文件标签、旧会话路径、队列等相当一部分仍位于 legacy 路径，恢复时必须接入 D Code 的产品合同。

| 能力 | 当前 Web 已有部分 | 尚未接入部分与证据 |
|---|---|---|
| 文件工作区与编辑 | 附件快速查看、图片放大、消息 Markdown、报告侧栏正文 | 项目文件树、文件多标签、行定位、Markdown 编辑/保存冲突、HTML 编辑与即时预览。旧实现见 WorkspaceContentView（历史源码 `app/Sources/PiDCode/Views/WorkspaceContentView.swift`，退役前 Git 基线 `18e12df`）、WorkspaceHTMLPreviewView（历史源码 `app/Sources/PiDCode/Views/WorkspaceHTMLPreviewView.swift`，退役前 Git 基线 `18e12df`）；不是已发送附件预览缺失。 |
| Git 工作区与变更 | 顶部项目分支查询 | 仓库状态、文件 diff/hunk 与精确引用。旧实现见 WorkInspectorView（历史源码 `app/Sources/PiDCode/Views/WorkInspectorView.swift`，退役前 Git 基线 `18e12df`）。完整提交历史没有在本次核验中找到可用实现证据，应作为规划缺口另列。 |
| 会话路径与编辑重走 | 普通续聊、复制会话、轮次导航、复制与引用消息 | 路径列表/切换、编辑旧用户消息后重走、从历史助手节点续写。旧 SessionPathSheet（历史源码 `app/Sources/PiDCode/Views/SessionPathSheet.swift`，退役前 Git 基线 `18e12df`） 和 ConversationView 有呈现；Host 的旧 `session.prompt.pathAction` 已存在，但 Web 使用的 `dcodeSession.prompt` 没有接入，不能越过原生会话权威直接复用。 |
| 运行中输入与队列 | 草稿保留、停止，运行结束后发送 | 当前 Web Composer 的发送条件排除 `work.running`；没有旧 Swift 的 steer 介入、后续消息队列编辑/排序/暂停恢复控制。旧实现见 ComposerView（历史源码 `app/Sources/PiDCode/Views/ComposerView.swift`，退役前 Git 基线 `18e12df`）。0.0.31 需按新的主对话/成员输入合同解决，不只搬旧队列面板。 |
| 技能、命令与通用扩展交互 | 本机资源管理及启停，D Code 原生选择请求和任务验收反馈 | Composer 的技能/命令菜单、斜杠发现与插入；扩展 select/confirm/input/editor 通用交互桥。旧实现见 ComposerView、ExtensionViews（历史源码 `app/Sources/PiDCode/Views/ExtensionViews.swift`，退役前 Git 基线 `18e12df`）。TUI Widget/custom 仍是明确排除项，不能以迁移名义恢复。 |
| 已有项目维护 | 新建项目、选择目录、任务归属 | 已有项目改名、更换目录、关联会话 cwd 迁移与可选文件搬迁。旧 ProjectEditorView（历史源码 `app/Sources/PiDCode/Views/ProjectEditorView.swift`，退役前 Git 基线 `18e12df`） 基于旧项目/会话模型，不能直接改原生 Project 记录冒充完成；这也不等于 后续待排期的多目录项目已经实现。 |

还需单独登记三类事项：

- **原生主路径的未收口能力**：通用任务上下文选择、运行环境/工具与来源回执检查，以及 Artifact 对应文件正文阅读。`FoundationConsoleView` 中存在部分数据操作视图，但本次未找到它在当前应用的调用入口；Swift `TaskWorkspaceContentView` 对 Artifact 也只展示身份、摘要和元数据，Web Inspector 同样没有通用文件读取。因此这些不能笼统归因于 Web 把完整功能删掉。报告正文和灵感版本引用已经可用。
- **工程退役**：已替换的 Swift 导航/对话/设置、旧状态与桥接路径、旧打包入口仍待按依赖拆除；品牌母版与 Host 共享合同须保留或明确迁移，不能直接删除整个 `app/`。
- **发布与验证**：签名、公证、正式分发、更新完整链路和此前未完成的原生物理拖放走查，分别属于发行或验证任务。终端也是规划能力，本次没有找到旧 Swift 终端实现，不应列成漏迁功能。

下一版的范围关系见 [PRD 0029](0029-任务内自适应协作产品需求.md)。该清单只明确当前边界，不自动把所有遗留项加入 0.0.31。
